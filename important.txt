# frontend
- create component
ng generate component [name] --inline-templete

- create service


- run the app
ng serve -o

# backend
- to run:
mvn spring-boot:run

- build & clean
mvn clean install

- build images for all microservices 
 sudo mvn clean package jib:dockerBuild